# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Runtime config (contains user-specific sensitive settings)
config.json

# 乐享 OpenAPI 凭证（虽然默认存放在 ~/.lexiang/，不会出现在本仓库，
# 但保险起见把常见误放位置都 ignore 掉）
.lexiang/
openapi.json
token_cache.json
